---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install packages
  apt:
    name:
      - curl
      - jq
      - nftables
      - wireguard
      - tcpdump
    state: present
